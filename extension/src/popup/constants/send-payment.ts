export enum STEPS {
  SET_SOURCE_ASSET = "set-source-asset",
  AMOUNT = "set-amount",
  DESTINATION = "set-destination",
  PAYMENT_CONFIRM = "payment-confirmation",
  SET_DESTINATION_ASSET = "set-destination-asset",
}

export enum MemoEditingContext {
  Review = "review",
  SendPayment = "sendPayment",
}
