name: tesst
on: [pull_request]
jobs:
  teast:
    name: teest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 16.14.0
      - run: yarn install
      - run: yarn test:ci
