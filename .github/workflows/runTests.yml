name: Translate and Test
on:
  pull_request:
    paths:
      - "extension/**/*"
jobs:
  test-ci:
    name: test
    runs-on: ubuntu-latest
    steps:
      - run: yarn install
      - run: yarn build:freighter-api
      - run: yarn test:ci
